name: Say workFlow Initial
on:
  push:
    branches: ["main"]
jobs:
  firts_workFlow:
    runs-on: ubuntu-latest
    steps:
      - name: Echo terminal
        run: echo "terminal"
      - name: Dime la fecha
        run: date
      - run: ls -l
